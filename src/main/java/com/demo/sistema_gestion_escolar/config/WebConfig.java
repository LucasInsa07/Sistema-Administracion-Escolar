package com.demo.sistema_gestion_escolar.config;

import org.springframework.context.annotation.Configuration;
import org.springframework.web.servlet.config.annotation.WebMvcConfigurer;

/**
 * Configuración mínima de MVC para el sistema de gestión escolar.
 * Solo backend - frontend eliminado
 */
@Configuration
public class WebConfig implements WebMvcConfigurer {
    // Configuración mínima - todos los recursos estáticos han sido eliminados
}
